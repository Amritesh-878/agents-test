- 2026-04-22: Orchestration started on branch main.
- MASTER_PLAN.md present; TASK-XXX.md files absent.
- orchestrator-temp.md created as user interaction channel.
- 2026-04-22: `.vscode/planned/whisperx/` contains TASK-001 through TASK-006; aligned titles and execution notes with root MASTER_PLAN.md.
- 2026-04-22: Root and planned master plans now both state TASK-003 and TASK-004 are independent outputs but should execute sequentially on the local RTX 3050 4GB GPU.
- 2026-04-22: Task worktrees may start from the tracked initial commit only; bootstrap any untracked planning files from the source repo before implementing the task.
- 2026-04-22: TASK-001 implemented, merged to main, and validated on trunk with ruff, mypy, pytest, and scripts/validate_env.py passing.
- 2026-04-29: TASK-002 completed in worktree `task-002-audio-extraction`; `scripts/extract_audio.py` was validated against the real Zoom MP4 from the provided ZIP and produced a 16kHz mono WAV.
- 2026-04-29: TASK-003 is unblocked by the audio extractor; TASK-004 still additionally requires a worktree `.env` with a valid HF token and accepted pyannote agreements.
- 2026-04-29: TASK-003 merged to `main` as squash commit `be1acdc` from worktree `task-003-transcription`; `ruff check --fix .`, `python -m mypy .`, and `py -3.10 -m pytest` passed in the isolated merge worktree.
- 2026-04-29: Post-merge `py -3.10 scripts/validate_env.py` did not fully validate on this host because CUDA was unavailable and the local 3.10 environment lacked `pyannote` and `whisperx`; the TASK-003 and temporary merge worktrees were removed after push.
- 2026-04-30: TASK-004 merged to remote `main` as squash commit `0f81aff` from worktree `task-004-diarization`; `py -3.10 -m ruff check --fix .`, `py -3.10 -m mypy .`, and `py -3.10 -m pytest` passed in an isolated merge worktree with 43 tests passing.
- 2026-04-30: Post-merge `py -3.10 scripts/validate_env.py` in the TASK-004 merged tree failed on this host because CUDA was unavailable, no HF token was configured, and `whisperx` was not installed; the temporary merge worktree was removed, but the source task worktree was retained because it contains ignored local files including `.env` and generated output.
- 2026-04-30: TASK-005 in worktree `task-005-merge` validates with `py -3.10`; `scripts/merge.py` needs a repo-root `sys.path` insert when run directly because it imports sibling task models from `scripts.transcribe` and `scripts.diarize`.
- 2026-04-30: On this host, WhisperX import requires `USE_TF=0` and `TRANSFORMERS_NO_TF=1` to avoid a TensorFlow/protobuf import failure inside `transformers`; a bounded 60-second clip from the provided ZIP was sufficient for real TASK-005 runtime validation.
- 2026-04-30: TASK-005 merged to remote `main` as squash commit `527251e` from source worktree `task-005-merge`; `py -3.10 -m ruff check --fix .`, `py -3.10 -m mypy .`, and `py -3.10 -m pytest` passed in temporary worktree `merge-task-005-main` with 52 tests passing.
- 2026-04-30: The temporary merge worktree for TASK-005 was removed after push and `git gc --prune=now`; the source task worktree was retained because it still contains ignored local artifacts in `output/` and cache directories.
- 2026-04-29: Completed task worktrees and local task branches for TASK-001 through TASK-005 were force-removed after the user asked for cleanup; only the root repo checkout remains registered.
- 2026-04-29: A clean verification worktree based on `origin/main` passed `ruff check --fix .`, `mypy .`, `pytest` (52 passed), and CLI help smoke checks for `extract_audio.py`, `transcribe.py`, `diarize.py`, and `merge.py` using the existing populated project virtual environment.
- 2026-04-29: Fresh dependency installation into a brand new Python 3.11 verification venv failed on `av==10.*` while building PyAV, so clean-install verification on this host currently depends on the already-working project environment rather than a fresh venv.
- 2026-04-29: The real attendance CSV available for TASK-006 contains only name, email, total duration, and guest fields; it does not include join/leave timestamps, so exact attendance windows and exact missed segments cannot be computed from that file alone.
- 2026-04-29: The participant-specific audio files in the Zoom ZIP are useful for speaker mapping and QA, but they do not replace join/leave timestamps for exact attendance-window reconstruction.
- 2026-05-20: TASK-006 merged to `origin/main` as squash commit `f48f777`; merged tree validation passed with `ruff check --fix .`, `mypy .`, and `pytest` (62 passed) in the isolated merge worktree.
- 2026-05-20: Final user-review artifacts were regenerated in `c:\Users\ansh\Desktop\ISL\Agents_test-worktrees\verify-main-clean\output` from the merged scripts and real project data.
- 2026-05-20: Final full-recording runtime artifacts available for inspection: `audio.wav`, `transcript_raw.json`, `diarization.json`, `transcript_diarized.json`, `student_contexts.json`, `student_context_review.md`, and `student_context_segments.csv`.
- 2026-05-20: Final runtime summary from merged scripts: 17m08s audio, 194 transcript segments, 2 diarized speakers, 353 diarization segments, 194 merged transcript segments with 1 UNKNOWN, and 48 participant contexts in `duration_only_estimated` mode with 2 mapped speakers.
- 2026-05-20: Full-recording diarization on GPU OOMed in the verification checkout; rerunning `scripts/diarize.py --allow-cpu` with `CUDA_VISIBLE_DEVICES=-1` completed successfully and allowed final artifact generation.
- 2026-05-20: TASK-006 completed in worktree `task-006-context-builder` with `scripts/build_context.py`, `data/sample_attendance.csv`, and review artifacts at `output/student_context_review.md` plus `output/student_context_segments.csv`; the approved duration-only fallback marks attendance windows and missed segments as estimated when join/leave timestamps are absent.
- 2026-05-20: TASK-006 real-data validation used the provided attendance CSV plus a bounded 60-second clip cut from `video1031110282.mp4` inside the supplied Zoom ZIP; that clip diarized as a single speaker, so only one participant was auto-mapped in the validation pass.
- 2026-05-20: TASK-006 merged to remote `main` as squash commit `f48f777` from source branch `task-006-context-builder`; in an isolated temporary merge worktree, `c:\Users\ansh\Desktop\ISL\Agents_test\.venv\Scripts\python.exe -m ruff check --fix .`, `-m mypy .`, and `-m pytest` all passed before push.
- 2026-05-20: Post-merge cleanup removed both the temporary merge worktree and the source worktree `task-006-context-builder`, deleted the temporary local merge branch, and ran `git gc --prune=now`; the root checkout on local `main` was left untouched and remains behind `origin/main` with local untracked files.
- 2026-05-20: `.vscode/planned/chatbot/` was normalized into a full Phase 2 plan for TASK-007 through TASK-010 with inspectability-first requirements; TASK-010 already existed in the repo and was rewritten rather than created from scratch.
- 2026-05-20: Phase 2 planning now requires each task to preserve reviewable provenance for speaker mapping, retrieved chunks, and student-specific relevance through chunk exports, retrieval debug artifacts, chat session traces, and golden-truth evaluation outputs.
- 2026-05-20: TASK-008 merged to `origin/main` as squash commit `3098243` from source branch `task-008-retrieval`; in isolated temporary worktree `merge-task-008-main`, `c:\Users\ansh\Desktop\ISL\Agents_test\.venv\Scripts\python.exe -m ruff check --fix .`, `-m mypy .`, and `-m pytest` all passed before push, with 83 tests passing.
- 2026-05-20: Post-merge cleanup removed the temporary merge worktree and deleted the temporary local merge branch, but retained source worktree `task-008-retrieval` because it still contains ignored runtime artifacts under `output/retrieval_debug` and copied vector data under `data/chroma`.
- 2026-05-20: TASK-009 merged to `origin/main` as squash commit `892bd3b` from source branch `task-009-chat`; in isolated temporary worktree `merge-task-009-main-v1`, `c:\Users\ansh\Desktop\ISL\Agents_test\.venv\Scripts\python.exe -m ruff check --fix .`, `-m mypy .`, and `-m pytest` all passed before push, with 90 tests passing.
- 2026-05-20: Post-merge cleanup removed the temporary merge worktree and deleted the temporary local merge branch for TASK-009, but retained source worktree `task-009-chat` because it still contains ignored runtime artifacts under `output/chat_sessions` and copied vector data under `data/chroma`.
- 2026-05-20: TASK-010 completed in worktree `task-010-eval` with `scripts/evaluate.py`, `data/eval_qa.json`, and `tests/test_evaluate.py`; bounded real validation against `..\task-007-chunk-embed\data\chroma` wrote `output\evaluation\summary.json`, `summary.md`, and failed-case artifacts with a `1 passed / 3 failed` split across retrieval, provenance, and chat-generation stages.
- 2026-05-21: TASK-010 merged to `origin/main` as squash commit `0a70b7f` from source branch `task-010-eval`; in isolated temporary worktree `merge-task-010-main-v1`, `c:\Users\ansh\Desktop\ISL\Agents_test\.venv\Scripts\python.exe -m ruff check --fix .`, `-m mypy .`, and `-m pytest` all passed before push, with 94 tests passing.
- 2026-05-21: Post-merge cleanup removed the temporary merge worktree and deleted the temporary local merge branch for TASK-010, but retained source worktree `task-010-eval` because it still contains ignored runtime artifacts under `output/evaluation`, including `summary.json`, `summary.md`, and failed-case outputs.
